import { CardsList } from '../../features';
import { Profile } from '../../widgets';

const HomePage = () => {
  return (
    <main>
      <Profile />
      <CardsList />
    </main>
  );
};

export default HomePage;
